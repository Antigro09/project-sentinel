"""X64H: hidden-convention semantic induction.

X64G established that a self-generated controlled language cannot show
induction beating authoring: whoever writes the realiser can write its
inverse, and a perfect inverse cannot be beaten. X64H removes that ceiling
by withholding the realised convention from every arm except the oracle.

This package is the IMPLEMENTATION SLICE. Passing its software criteria is
not passing X64H -- the research result is decided only by H1-H10 on
conventions sampled after the freeze manifest is committed, and no final
seed may be sampled or inspected before then.
"""
